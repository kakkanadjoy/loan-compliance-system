# Serving dependencies ONLY — what the running API needs.
# Dev/training tools stay in requirements.txt and out of the image.
# Discipline: new imports in shipped code (app/, rag/, agent/) get added here too.

fastapi>=0.110
uvicorn>=0.29
pydantic-settings>=2.2
pyyaml>=6.0
numpy>=1.26

# policy store + hybrid search
psycopg[binary]>=3.1
pgvector>=0.3
sentence-transformers>=3.0

# PII redaction
presidio-analyzer>=2.2
presidio-anonymizer>=2.2

# LLM memo drafter
openai>=1.40
langgraph>=0.2